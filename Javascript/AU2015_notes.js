//On an HTML page:
	//Hardcoded function - 
		<head> <script> function Hah() {
		}
		</script></head>
	//Refer to a file - 
		<body> <script scr = "MyScript.js"> 
		</script> </body>

//AJAX = asynchronous javascript XML
	//Allows for background communication with web page server rather than reloading the page each time
	
//CSS Styles
	//P{} = element
	//#para{} = id
	//.center{} = class

//JSON
	Object {string:value,string:value}
//Promise advantage with then() means reuse functions across your code
	//Prior to then() you send arg to a different function which gets passed to the then() functions

Acad.application
	//Callback = start/ stop observer that reports all changes (record macro?)

Acad.editor
	//Store a command to a button (4 lines), use Async version if requires user action
	//AddEventListener calls back name, old value and new value

//Debugging inside AutoCAD
	//To start, hit f12 while hovering over the html page

//Extend JavaScript API:
	//Useful to hide info like passwords, company info, unique identifiers, etc
	//Allows you to create a Javascript method that calls a .Net callback unseen by the user
